import { Footer } from '../components/Footer';
import NavAccueil from '../components/NavBar';

export default function SuiviSommeil({ session }) {
  return (
    <div>
      <NavAccueil />
      <h1>sommeil</h1>
      <Footer />
    </div>
  );
}
